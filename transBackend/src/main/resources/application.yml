spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/worldtree
    username: root
    password: Hatsune Miku
  data:
    redis:
      host: localhost
      port: 6379

# application.yml
server:
  port: 8080
  address: 0.0.0.0


mybatis:
  configuration:
    map-underscore-to-camel-case: true
